## Managed dependencies — LVGL op de bestaande esp_lcd-route (ADR-004).
## ESP-IDF ≥6.0: ingebouwde `json`- en `mqtt`-componenten verwijderd — via Component Manager.
dependencies:
  espressif/esp_lvgl_port: "^2.5.0"
  espressif/cjson: "^1.7.19"
  espressif/mqtt: "^1.0.0"
